<Xpf:CustomControl  x:Class="SmartEMR.Application.Views.SmartEMRRES.ReservationItemTemplate"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                    xmlns:local="clr-namespace:SmartEMR.Application.Views.SmartEMRRES"
                    xmlns:Xpf="clr-namespace:SmartEMR.Application.Xpf"
                    xmlns:converter="clr-namespace:SmartEMR.Application.Common.Converter"
                    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
                    mc:Ignorable="d" 
                    d:DesignHeight="450" d:DesignWidth="800">
    
    <Border Padding="8,4"
            Margin="0,1"
            CornerRadius="4"
            Background="Transparent">

        <Border.Resources>
            <Style TargetType="Border">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                        <Setter Property="Background" Value="#D9ECFF"></Setter>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Resources>

        <Xpf:StyleGrid LayoutDefinitions="3,1"
                       ColumnSizeDefinitions="20,70,80">
            
            <Ellipse    Grid.Column="0"
                        Width="10"
                        Height="10"
                        VerticalAlignment="Center">
                
                <Ellipse.Style>
                    <Style TargetType="Ellipse">
                        <Setter Property="Fill" Value="#28A745"/>

                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                <Setter Property="Fill" Value="#007ACC"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding RESItem.PAT_Idx, Converter={converter:IntToBooleanConverter}}" Value="False">
                                <Setter Property="Fill" Value="#D9ECFF"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Ellipse.Style>
            </Ellipse>

            <Xpf:TextBlock  Grid.Column="1"
                            VerticalAlignment="Center"
                            FontSize="13"
                            FontWeight="SemiBold"
                            Text="{Binding vRES_Time, StringFormat={}{0:hh\\:mm}}"/>

            <Xpf:TextBlock  Grid.Column="2"
                            FontSize="13"
                            TextAlignment="Center">

                <Xpf:TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Text" Value="예약 가능"/>
                        <Setter Property="Foreground" Value="#28A745"/>

                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsReserved}" Value="True">
                                <Setter Property="Text" Value="{Binding RESItem.PAT_Name}"/>
                                <Setter Property="Foreground" Value="#333333"/>
                            </DataTrigger>

                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                <Setter Property="Text" Value="선택됨"/>
                                <Setter Property="Foreground" Value="#007ACC"/>
                                <Setter Property="FontWeight" Value="SemiBold"></Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Xpf:TextBlock.Style>
            </Xpf:TextBlock>
        </Xpf:StyleGrid>
    </Border>
</Xpf:CustomControl>
